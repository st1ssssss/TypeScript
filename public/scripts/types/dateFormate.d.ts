export declare const isoDateTransform: (date: number) => string;
export declare const dateFormate: (date: number) => {
    currentDate: string;
    departureDate: string;
    maxDate: string;
};
